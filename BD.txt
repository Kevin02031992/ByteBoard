CREATE DATABASE IF NOT EXISTS db_byteboard;
USE db_byteboard;

CREATE TABLE IF NOT EXISTS user (
  user_id VARCHAR(10) PRIMARY KEY,   -- ID string de longitud 10
  user_name VARCHAR(100) NOT NULL,   -- Nombre del usuario
  user_mail VARCHAR(100) NOT NULL    -- Correo electrónico del usuario
);